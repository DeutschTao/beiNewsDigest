# Bei News Digest v2 - 小程序端技术方案

| 属性 | 内容 |
|------|------|
| **产品名称** | Bei News Digest - 小程序端 |
| **文档类型** | 前端技术方案设计（v2 重构版） |
| **版本** | v2.0 |
| **创建日期** | 2026-07-23 |
| **状态** | 设计阶段，待实施 |
| **对应目录** | `wemini/`（沿用 v1 目录，仅作内容改动） |

---

## 〇、本版本与 v1 的差异总览

| 维度 | v1 | v2 |
|------|------|------|
| **API 前缀** | `/api/*` | **`/api/v2/*`**（域名复用，仅路径前缀切换） |
| **页面数量** | 6 个（home/detail/ai/all-news/sources/settings） | **5 个**（去掉 `pages/ai/`，因为去除 AI 摘要） |
| **首页模型** | 横滑卡片，1 个 8 条精选 | **按来源分组**，每源顶置 3 条（5×3 ≈ 15 条），组内滑动或整页滚动 |
| **首页交互** | 全屏 Swiper 横切 | **整页垂直滚动**，每源为 section，每个 section 内横向滑动 3 张卡片 |
| **详情页** | 顶部摘要 + 底部 AI 分析按钮 | **首页摘要 + 按需爬原文**（D + C 兜底）；不再跳 AI 页 |
| **新闻源管理** | 只支持添加 RSS | **新增类型选择**：RSS 源 / HTML 源 两类 |
| **设置** | 推送配置 + 新闻数量 + 源管理 | **去掉"新闻数量"**（首页固定按源 Top3）；推送保留 UI 占位，后端未实现；其他不变 |
| **图片代理** | 全量代理 | **保留**，但仅在 403/防盗链时使用，正常源不再强制走代理 |

---

## 一、技术栈

v1 已经选定，本版本**技术栈不变**：

| 层级 | 选型 | 说明 |
|------|------|------|
| 框架 | 微信原生小程序 | 沿用 v1 |
| 状态 | 页面内 `data` + `wx.storage` | 沿用 v1，无须引入额外框架 |
| HTTP | 自封装 `services/api.js` | 沿用 v1，仅切换 base URL |
| 时间 | `utils/date.js` | 沿用 v1 |
| 存储 | `utils/storage.js` | 沿用 v1 |

**不引入新依赖**。

---

## 二、目录结构

```
wemini/                                 # 沿用 v1 目录
├── app.js                              # 沿用，仅修改 globalData
├── app.json                            # 删掉 pages/ai 配置
├── app.wxss                            # 沿用
├── sitemap.json                        # 沿用
├── project.config.json                 # 沿用
│
├── pages/                              # 5 个页面（删 ai 目录）
│   ├── home/                           # ← 重点改动
│   │   ├── home.js
│   │   ├── home.wxml
│   │   ├── home.wxss
│   │   └── home.json
│   ├── all-news/                       # 仅调整 API 调用层
│   │   ├── all-news.js
│   │   ├── all-news.wxml
│   │   ├── all-news.wxss
│   │   └── all-news.json
│   ├── detail/                         # ← 重点改动
│   │   ├── detail.js
│   │   ├── detail.wxml
│   │   ├── detail.wxss
│   │   └── detail.json
│   ├── sources/                        # ← 重点改动（添加类型选择）
│   │   ├── sources.js
│   │   ├── sources.wxml
│   │   ├── sources.wxss
│   │   └── sources.json
│   └── settings/                       # ← 删除"新闻数量"
│       ├── settings.js
│       ├── settings.wxml
│       ├── settings.wxss
│       └── settings.json
│
├── services/
│   ├── api.js                          # ← 改：API_BASE 改 /api/v2
│   └── news.js                         # ← 改：删除 AI 相关调用，新增 home() / addCustomSource(type, ...)
│
├── components/                         # 全部沿用 v1，本版本不新增组件
│   ├── atoms/                          # 不再使用，可保留不删
│   ├── digest-card/
│   ├── digest-footer/
│   ├── digest-header/
│   ├── empty-state/
│   ├── fab-menu/
│   ├── progress-indicator/
│   └── skeleton-card/
│
├── utils/
│   ├── date.js                         # 沿用
│   ├── index.js                        # 沿用
│   └── storage.js                      # ← 改：去掉 newsCount 字段
│
└── styles/
    ├── animations.wxss                 # 沿用
    ├── common.wxss                     # 沿用
    └── variables.wxss                  # 沿用
```

**删除文件**：
- `pages/ai/`（整个目录）

---

## 三、API 前缀切换（核心改动）

### 3.1 `services/api.js`

```js
// wemini/services/api.js
const BASE_URL = 'https://your-aliyun-domain.com';  // 不变（已部署域名）

module.exports = {
  API_BASE: `${BASE_URL}/api/v2`,    // ← 改动：v1 是 `/api`，v2 是 `/api/v2`
  // ... 其余方法（request/get/post/delete/patch）全部沿用
};
```

仅此一处，所有页面调用 `api.get('/news/{id}')` 自动变成 `GET /api/v2/news/{id}`，无需改每个文件的相对路径。

### 3.2 域名白名单

**不变**：微信公众平台 → 小程序后台 → 服务器域名已经加了 `your-aliyun-domain.com`，前端无需任何修改。

---

## 四、页面改动详解

### 4.1 首页（home）——按来源分组

#### 4.1.1 设计目标

v1 首页是横滑卡片（1 个 swiper，连续切 8 张）。v2 首页变为**按来源分组**，每个源固定展示 3 条，**整体上下滚动**，每个组内部可横滑。

#### 4.1.2 数据模型

```js
// Page.data 结构
{
  groups: [
    {
      source_id: 1,
      source_code: 'bbc',
      source_name: 'BBC News',
      items: [
        { id, title, summary, cover_image, published_at, url },
        { id, title, summary, cover_image, published_at, url },  // 3 条
        { id, title, summary, cover_image, published_at, url },
      ],
    },
    {
      source_id: 2,
      source_code: 'cnn',
      source_name: 'CNN World',
      items: [ ... 3 条 ... ],
    },
    // ... 5 个源
  ],
  loading: false,
  lastFetchTime: '',
  currentDate: '',
}
```

#### 4.1.3 接口对接

调用 `services/news.js#getHome()`，内部对应 `GET /api/v2/home`：

```js
// services/news.js 新增方法
getHome() {
  return api.get('/home');
}
```

后端返回的 `data.groups` 字段已按源分组（含 `items: [...]`），前端**无需再分组**，直接 `setData({groups: data.groups})` 即可。

#### 4.1.4 视图（wxml 改动）

v1：整页只有一个 `swiper`，8 个 swiper-item。

v2：整页是一个 `scroll-view`，每个 group 是一个 section，section 内部有一个水平 swiper（3 张卡片）：

```html
<view class="home-page">
  <digest-header date="{{currentDate}}" lastUpdate="{{lastFetchTime}}" />

  <view wx:if="{{loading}}" class="loading-overlay">
    <skeleton-card />
  </view>

  <scroll-view wx:elif="{{groups.length > 0}}" scroll-y class="home-scroll">
    <!-- 每个来源一个 section -->
    <view wx:for="{{groups}}" wx:key="source_id" wx:for-item="group" class="source-section">
      <!-- 来源头部 -->
      <view class="section-header">
        <view class="source-logo">
          <text>{{group.source_name[0]}}</text>
        </view>
        <text class="source-name">{{group.source_name}}</text>
        <text class="section-count">{{group.items.length}} 条</text>
      </view>

      <!-- 水平卡片轮播（3 张） -->
      <swiper class="section-swiper" circular="{{false}}" duration="{{300}}">
        <swiper-item 
          wx:for="{{group.items}}" 
          wx:key="id"
          wx:for-item="news"
          class="section-item"
        >
          <view class="news-card" bindtap="onCardTap" data-news="{{news}}">
            <image 
              class="news-bg {{news._imgError ? 'hidden' : ''}}"
              src="{{news.cover_image}}"
              mode="aspectFill"
              binderror="onImageError"
              data-id="{{news.id}}"
            />
            <view wx:if="{{news._imgError}}" class="news-placeholder">
              <text>📰</text>
            </view>
            <view class="news-overlay"></view>
            <view class="news-content">
              <text class="news-title">{{news.title}}</text>
              <view class="news-meta">
                <text class="news-time">{{news._formattedTime}}</text>
              </view>
            </view>
          </view>
        </swiper-item>
      </swiper>

      <!-- 该源所有新闻按钮 -->
      <view class="section-footer" bindtap="onSeeAll" data-source="{{group}}">
        <text>查看 {{group.source_name}} 全部 →</text>
      </view>
    </view>
  </scroll-view>

  <view wx:else class="empty-state-wrap">
    <empty-state
      type="no-data"
      title="暂无新闻"
      description="下拉刷新获取最新新闻"
      showAction="{{true}}"
      actionText="刷新"
      bind:action="onRefreshTap"
    />
  </view>

  <digest-footer
    total="{{newsCount}}"
    showProgress="{{false}}"
    lastRefresh="{{lastFetchTime}}"
  />

  <fab-menu bind:action="onFabAction" />
</view>
```

#### 4.1.5 关键交互细节

1. **横滑只在该 source section 范围内**，不跨 section
2. **"查看全部"按钮**：跳 `pages/all-news/all-news?source_id={id}`
3. **下拉刷新**：触发 `triggerFetch`（异步）后调 `fetchHome()` 重新拉数据
4. **右下角 FAB**：
   - 全部新闻：去 `all-news`（不筛 source）
   - 刷新：`triggerFetch` + `fetchHome`
   - 设置：去 `settings`
5. **空状态**：当所有源都空时显示空态组件（v1 已有）
6. **滚动到底部时的 FAB 自动隐藏**：沿用 v1 的 `fab-menu` 实现

#### 4.1.6 JS 改动要点（home.js）

```js
const newsService = require('../../services/news.js');

Page({
  data: {
    groups: [],
    loading: true,
    lastFetchTime: '',
    currentDate: '',
    newsCount: 0,         // 5 个源 × 3 条 ≈ 15
  },

  onLoad() {
    this.setCurrentDate();
    this.fetchHome();
  },

  async fetchHome() {
    this.setData({ loading: true });
    try {
      const res = await newsService.getHome();
      const data = res?.data || res;
      if (data && Array.isArray(data.groups)) {
        // 格式化时间（也可用 formatTime 工具）
        data.groups.forEach(g => {
          g.items.forEach(item => {
            item._formattedTime = this._formatTime(item.published_at);
          });
        });
        this.setData({
          groups: data.groups,
          loading: false,
          lastFetchTime: Date.now(),
          newsCount: data.groups.reduce((s, g) => s + g.items.length, 0),
        });
      } else {
        this.setData({ groups: [], loading: false });
      }
    } catch (err) {
      console.error('fetch home failed', err);
      this.setData({ loading: false });
    }
  },

  // 触发后端抓取（异步），1.5s 后重拉首页
  async onRefresh() {
    try {
      await newsService.triggerFetch();
      setTimeout(() => this.fetchHome(), 1500);
    } catch (err) {
      wx.showToast({ title: '刷新失败', icon: 'none' });
    }
  },

  onPullDownRefresh() {
    this.fetchHome().then(() => wx.stopPullDownRefresh());
  },

  onCardTap(e) {
    const { news } = e.currentTarget.dataset;
    if (news && news.id) {
      wx.navigateTo({ url: `/pages/detail/detail?id=${news.id}` });
    }
  },

  onSeeAll(e) {
    const { source } = e.currentTarget.dataset;
    wx.navigateTo({ url: `/pages/all-news/all-news?source_id=${source.source_id}` });
  },

  onFabAction(e) {
    const { action } = e.detail;
    if (action === 'allNews') wx.navigateTo({ url: '/pages/all-news/all-news' });
    else if (action === 'refresh') this.onRefresh();
    else if (action === 'settings') wx.navigateTo({ url: '/pages/settings/settings' });
  },
});
```

#### 4.1.7 复用 v1 的原子组件

- `digest-header`：依然放在顶部
- `digest-footer`：依然放在底部（可能去掉进度圆点，因为不再是横滑 8 张；改为显示"X 个来源 / Y 条新闻"）
- `fab-menu`：原样保留
- `empty-state` + `skeleton-card`：原样保留

---

### 4.2 详情页（detail）——展示原文

#### 4.2.1 设计目标

v1 详情页：列表摘要 + 跳 AI 分析。

v2 详情页：**展示首页摘要 + 按需爬正文（如果摘要 < 100 字）**。

#### 4.2.2 数据模型

```js
{
  newsId: 456,
  detail: {
    id: 456,
    title: '...',
    source_name: 'BBC News',
    source_code: 'bbc',
    cover_image: '...',
    url: '...',
    published_at: '...',
    
    summary: '...',                // 列表摘要（兜底渲染）
    has_full_content: true,         // 后端是否抓到了详情页
    content_html: '<article>...</article>',  // 可选
    content_source: 'homepage' | 'fetched',
    fetched_at: '...',              // 详情页抓取时间
  },
  contentSource: 'homepage',   // 'homepage' | 'fetched'
  formattedTime: '...',
  loading: true,
  error: '',
  coverError: false,
}
```

#### 4.2.3 接口对接

```js
// services/news.js
getNewsDetail(id) {
  return api.get(`/news/${id}`);   // GET /api/v2/news/{id}
}
```

#### 4.2.4 视图（detail.wxml 改动）

v2 详情页分两种渲染模式：

**模式 1**：后端抓到全文（`has_full_content=true`）—— 渲染 `content_html`

**模式 2**：没抓到全文 —— 展示 `summary`，底部加"查看原文链接"按钮（沿用 v1）

```html
<view class="detail-page">
  <view wx:if="{{loading}}" class="loading-container">
    <view class="loading-spinner"></view>
    <text>正在加载...</text>
  </view>

  <view wx:elif="{{error}}" class="error-container">
    <empty-state type="error" title="加载失败" description="{{error}}" showAction="{{true}}" actionText="重试" bind:action="loadNewsDetail" />
  </view>

  <view wx:else class="detail-content">
    <!-- 封面图 -->
    <view class="cover-section">
      <image wx:if="{{detail.cover_image && !coverError}}" src="{{detail.cover_image}}" mode="aspectFill" class="cover-image" binderror="onCoverError" bindtap="onPreviewCover" />
      <view wx:else class="cover-placeholder"><text>📰</text></view>
      <view class="cover-overlay"></view>
    </view>

    <!-- 头部信息 -->
    <view class="news-header">
      <text class="news-title">{{detail.title}}</text>
      <view class="news-meta">
        <text class="news-source">{{detail.source_name}}</text>
        <text class="meta-dot">·</text>
        <text class="news-time">{{formattedTime}}</text>
      </view>
    </view>

    <!-- 内容区：根据 contentSource 决定渲染 -->
    <view class="content-section">
      <view class="content-label">
        <text class="label-icon">{{contentSource === 'fetched' ? '📄' : '📝'}}</text>
        <text class="label-text">
          {{contentSource === 'fetched' ? '原文内容' : '摘要'}}
          <text wx:if="{{contentSource === 'fetched'}}" class="fetched-tag">· 实时抓取</text>
        </text>
      </view>

      <!-- 模式 1：渲染后端返回的富文本 -->
      <rich-text wx:if="{{detail.has_full_content}}" nodes="{{detail.content_html}}" class="content-body" />

      <!-- 模式 2：展示 summary + 原文链接 -->
      <view wx:else class="content-body">
        <text class="content-text">{{detail.summary || '暂无内容'}}</text>
      </view>
    </view>

    <!-- 原文链接（始终显示） -->
    <view class="original-link" bindtap="onOpenOriginal">
      <text class="link-icon">🔗</text>
      <text class="link-text">查看原文链接</text>
      <text class="link-arrow">→</text>
    </view>

    <!-- 底部空（去掉 AI 按钮） -->
  </view>
</view>
```

#### 4.2.5 关键交互细节

1. **`rich-text` 渲染 content_html**
   - 后端已经清理过（去 nav/aside/ad/script/style），但仍可能含相对路径的资源
   - 图片代理：rich-text 内的 `<img>` 由 wx 自动加载；
     - 当前不需要加额外处理（v1 经验：BBC/AP 主页拿到的图片基本都能直接访问）
     - 如遇防盗链，可在后端 rich-text 处理时将 `<img src="...">` 重写为 `/api/v2/proxy/image?url=...`
     - **v2 暂不做**，等发现真的有问题再加
2. **`content-html` 富文本中可能的 `<a>` 标签**：rich-text 默认支持，但点击会走 web-view 跳转；
   - 不强制覆盖，v1 没做
3. **`formatTime`**：沿用 `utils/date.js`

#### 4.2.6 JS 改动（detail.js）

```js
const newsService = require('../../services/news.js');
const { formatTime } = require('../../utils/date.js');

Page({
  data: {
    newsId: '',
    detail: null,
    contentSource: 'homepage',
    formattedTime: '',
    loading: true,
    error: '',
    coverError: false,
  },

  onLoad(options) {
    if (options.id) {
      this.setData({ newsId: options.id });
      this.loadNewsDetail();
    } else {
      this.setData({ loading: false, error: '缺少新闻 ID' });
    }
  },

  async loadNewsDetail() {
    this.setData({ loading: true, error: '', coverError: false });
    try {
      const res = await newsService.getNewsDetail(this.data.newsId);
      const detail = res?.data || res;
      if (!detail) throw new Error('未找到新闻');

      const adapt = {
        id: detail.id,
        title: detail.title,
        source_name: detail.source_name,
        source_code: detail.source_code,
        cover_image: detail.cover_image,
        url: detail.url || detail.content_url,
        published_at: detail.published_at || detail.publish_time,
        summary: detail.summary || '',
        has_full_content: !!detail.has_full_content,
        content_html: detail.content_html || '',
      };

      this.setData({
        loading: false,
        detail: adapt,
        contentSource: adapt.has_full_content ? 'fetched' : 'homepage',
        coverError: !adapt.cover_image,
        formattedTime: formatTime(adapt.published_at),
      });
    } catch (err) {
      console.error('load news detail failed', err);
      this.setData({ loading: false, error: '加载失败，请重试' });
    }
  },

  onPreviewCover() {
    const url = this.data.detail?.cover_image;
    if (url && !this.data.coverError) {
      wx.previewImage({ urls: [url], current: url });
    }
  },

  onCoverError() {
    this.setData({ coverError: true });
  },

  onOpenOriginal() {
    const url = this.data.detail?.url;
    if (url) {
      wx.setClipboardData({
        data: url,
        success: () => wx.showToast({ title: '链接已复制，请在浏览器打开', icon: 'none' }),
      });
    } else {
      wx.showToast({ title: '原文链接不可用', icon: 'none' });
    }
  },

  onBack() {
    wx.navigateBack();
  },
});
```

---

### 4.3 新闻源管理（sources）——新增类型选择

#### 4.3.1 设计目标

v1 sources 页只支持添加 RSS。

v2 sources 页**新增类型选择**：用户在添加自定义源时，**先选类型**（RSS 或 HTML），再填 URL。

#### 4.3.2 UI 改动

加一个 **类型选择** 卡片，位于"添加自定义源"区域上方：

```html
<!-- sources.wxml 片段：custom-tab 内容 -->
<view wx:if="{{activeTab === 'custom'}}" class="custom-tab">
  <!-- 类型选择卡（v2 新增） -->
  <view class="type-selector-card">
    <view class="selector-header">
      <text class="selector-title">选择源类型</text>
      <text class="selector-desc">不同类型使用不同的抓取方式</text>
    </view>
    
    <view class="type-options">
      <view 
        class="type-option {{sourceType === 'rss' ? 'active' : ''}}"
        bindtap="onSelectType"
        data-type="rss"
      >
        <text class="type-icon">📡</text>
        <view class="type-info">
          <text class="type-name">RSS 源</text>
          <text class="type-desc">推荐：支持标准 RSS/Atom</text>
        </view>
        <view wx:if="{{sourceType === 'rss'}}" class="type-check">✓</view>
      </view>

      <view 
        class="type-option {{sourceType === 'crawler' ? 'active' : ''}}"
        bindtap="onSelectType"
        data-type="crawler"
      >
        <text class="type-icon">🌐</text>
        <view class="type-info">
          <text class="type-name">网页爬虫</text>
          <text class="type-desc">通用 HTML 提取，效果有限</text>
        </view>
        <view wx:if="{{sourceType === 'crawler'}}" class="type-check">✓</view>
      </view>
    </view>
  </view>

  <!-- 添加卡（根据 sourceType 改变提示） -->
  <view class="add-card">
    <view class="add-header">
      <text class="add-icon">📝</text>
      <text class="add-title">
        添加自定义 {{sourceType === 'rss' ? 'RSS' : '网页'}}
      </text>
      <text class="add-desc">
        {{sourceType === 'rss' 
          ? '输入 RSS/Atom 订阅链接（推荐）' 
          : '输入网页 URL，仅抓取该页首页基础信息'}}
      </text>
    </view>
    <view class="input-wrapper">
      <input class="url-input" type="text" placeholder="源名称（选填，不填自动获取）" value="{{customName}}" bindinput="onNameInput" />
    </view>
    <view class="input-wrapper">
      <input class="url-input" type="text" placeholder="{{sourceType === 'rss' ? 'https://example.com/feed.xml' : 'https://example.com/news'}}" value="{{customUrl}}" bindinput="onUrlInput" />
    </view>
    <view class="submit-btn {{customUrl ? 'active' : ''}}" bindtap="onAddSource">
      <text wx:if="{{adding}}">添加中...</text>
      <text wx:else>确认添加</text>
    </view>
  </view>

  <!-- 已添加的自定义源（保持原样） -->
  <view wx:if="{{customSources.length > 0}}" class="custom-list">
    ...
  </view>
</view>
```

#### 4.3.3 data 与方法变动

```js
Page({
  data: {
    activeTab: 'preset',
    loading: true,
    adding: false,
    
    // 新增：源类型
    sourceType: 'rss',    // 'rss' | 'crawler'
    
    // 原有的
    customName: '',
    customUrl: '',
    presetSources: [],
    customSources: [],
  },

  onLoad() {
    this.loadSources();
  },

  // 新增：类型切换
  onSelectType(e) {
    const { type } = e.currentTarget.dataset;
    if (type !== this.data.sourceType) {
      this.setData({ sourceType: type, customUrl: '' });  // 切类型时清空 URL
    }
  },

  // 改动：传 type 参数
  async onAddSource() {
    if (!this.data.customUrl || this.data.adding) return;
    this.setData({ adding: true });
    try {
      const res = await newsService.addSource({
        name: this.data.customName || undefined,
        source_type: this.data.sourceType,
        url: this.data.customUrl,
      });
      const data = res?.data || res;
      if (data && data.id) {
        const newSource = {
          ...data,
          _status: null,
          _checking: false,
        };
        const customSources = [...this.data.customSources, newSource];
        this.setData({
          customSources,
          customUrl: '',
          customName: '',
          adding: false,
          sourceType: 'rss',  // 完成后重置
          activeTab: 'preset',
        });
        wx.showToast({ title: '添加成功', icon: 'success' });
      }
    } catch (err) {
      console.error('添加失败', err);
      this.setData({ adding: false });
      wx.showToast({
        title: '添加失败，请检查链接',
        icon: 'none',
        duration: 2000,
      });
    }
  },

  // 其余方法（loadSources/onCheckSource/onToggleSource/onDeleteSource）保持不变
});
```

#### 4.3.4 services/news.js 的方法签名变化

```js
// v1
addSource(rssUrl, name) {
  const data = { rss_url: rssUrl };
  if (name) data.name = name;
  return api.post('/sources/custom', data);
}

// v2
addSource({ name, source_type, url }) {
  const data = { source_type, url };
  if (name) data.name = name;
  return api.post('/sources/custom', data);
}
```

后端响应里 `source_type` 字段会带回来，前端可以在列表展示时区分（可选）。

#### 4.3.5 列表展示微调（可选）

`loadSources` 时增加 `source_type` 字段的展示，可以让用户区分 RSS 源和 HTML 源。简单实现：在源名称旁加一个小 badge：

```html
<view class="source-name">
  {{item.name}}
  <text wx:if="{{item.source_type === 'crawler'}}" class="badge-crawler">网页</text>
</view>
```

---

### 4.4 设置（settings）——删除"新闻数量"

#### 4.4.1 改动清单

| 元素 | v1 | v2 |
|------|----|----|
| 推送通知卡 | ✅ 保留 | ✅ 保留（UI 占位，后端未实现） |
| 早/晚报时间 | ✅ 保留 | ✅ 保留 |
| 首页推荐新闻数量 | ✅ 4/6/8/10/12 | **❌ 删除** |
| 新闻源管理 | ✅ 保留 | ✅ 保留 |
| 清除缓存 | ✅ 保留 | ✅ 保留 |
| 关于 | ✅ 保留 | ✅ 保留 |
| 重置设置 | ✅ 保留 | **改：清除缓存 + 默认值变化** |

#### 4.4.2 删除项

**wxml**：删除"首页推荐新闻数量"那个 `<view class="setting-item">` 块。

**js**：
- 删除 `onSelectNewsCount` 方法
- 删除 `newsCount` 字段
- `loadSourceCounts` 调用方不变（接口加 `/api/v2/stats` 可选）

#### 4.4.3 utils/storage.js 改动

```js
// v1 默认设置
const defaultSettings = {
  pushEnabled: true,
  morningPush: { enabled: true, time: '06:00' },
  eveningPush: { enabled: true, time: '21:00' },
  newsCount: 8,    // v2 删除
};

// v2 默认设置（去掉 newsCount）
const defaultSettings = {
  pushEnabled: true,
  morningPush: { enabled: true, time: '06:00' },
  eveningPush: { enabled: true, time: '21:00' },
};
```

`updateSettings` / `setSettings` / `clearStorage` 函数**沿用 v1**，只是默认值变化。

#### 4.4.4 "重置设置" 行为变化

v1 重置会把所有配置改回默认值（含 newsCount=8）。v2 也按默认值重置，但不包含 newsCount。

---

### 4.5 全部新闻列表（all-news）——最小改动

#### 4.5.1 改动清单

- 调用 `services/news.js#getRawNews(page, pageSize)` **不变**（已经在调用 `/api/v2/news`）
- 不需要改 wxml/wxss
- **可选**：支持 `?source_id=` 查询参数，仅展示某源新闻（用于首页"查看全部"）

#### 4.5.2 source_id 筛选实现

```js
onLoad(options) {
  if (options.source_id) {
    this.setData({ sourceId: parseInt(options.source_id, 10) });
  }
  this.loadNews();
},

async loadNews() {
  // ...
  const res = await newsService.getRawNews(this.data.page, this.data.pageSize, this.data.sourceId);
  // ...
},
```

```js
// services/news.js
getRawNews(page = 1, pageSize = 20, sourceId = null) {
  const params = { page, page_size: pageSize };
  if (sourceId) params.source_id = sourceId;
  return api.get('/news', params);
}
```

该参数后端必须支持，详细见后端文档 `GET /api/v2/news` 的 `source_id` 查询参数。

---

## 五、services/news.js 方法清单（v2 完整版）

| 方法 | HTTP | 路径 | 说明 |
|------|------|------|------|
| `getHome()` | GET | `/api/v2/home` | 按来源分组的首页数据 |
| `getRawNews(page, pageSize, sourceId?)` | GET | `/api/v2/news?page=&page_size=&source_id=` | 全部新闻分页 |
| `getNewsDetail(id)` | GET | `/api/v2/news/{id}` | 新闻详情（含按需正文） |
| `getSources()` | GET | `/api/v2/sources` | 新闻源列表 |
| `addSource({name, source_type, url})` | POST | `/api/v2/sources/custom` | 添加自定义源 |
| `deleteSource(id)` | DELETE | `/api/v2/sources/{id}` | 删除 |
| `toggleSource(id)` | PATCH | `/api/v2/sources/{id}/toggle` | 启用/禁用 |
| `checkSource(id)` | POST | `/api/v2/sources/{id}/check` | 连通性检测 |
| `triggerFetch()` | POST | `/api/v2/trigger/fetch` | 手动触发抓取 |
| `triggerFetchSource(id)` | POST | `/api/v2/trigger/fetch/source/{id}` | 触发单源 |
| `getProxyImageUrl(url)` | - | - | 返回 `${API_BASE}/proxy/image?url=...` |

**删除的方法**（v1 有，v2 不要）：
- `getDigest`
- `triggerProcess`
- `triggerDigest`
- `triggerRefresh`（用 `triggerFetch` 代替）
- `processNews`
- `getRecommend`

---

## 六、视觉/样式细节

### 6.1 配色与主题

完全沿用 v1（深色毛玻璃 + Apple 风格），不引入新色彩变量。

### 6.2 首页新增样式

```css
/* home.wxss 新增片段 */
.home-page {
  height: 100vh;
  display: flex;
  flex-direction: column;
}

.home-scroll {
  flex: 1;
  height: calc(100vh - 200rpx);  /* 减去 header/footer */
}

.source-section {
  margin-bottom: 60rpx;
}

.section-header {
  display: flex;
  align-items: center;
  padding: 32rpx 40rpx 16rpx;
}

.source-logo {
  width: 64rpx;
  height: 64rpx;
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 24rpx;
  font-size: 28rpx;
}

.source-name {
  flex: 1;
  font-size: 32rpx;
  font-weight: 600;
  color: #fff;
}

.section-count {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.5);
}

.section-swiper {
  height: 520rpx;
  padding: 0 40rpx;
}

.section-item {
  display: flex;
}

.news-card {
  position: relative;
  width: 100%;
  border-radius: 32rpx;
  overflow: hidden;
  background: rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(20rpx);
}

.news-bg {
  width: 100%;
  height: 100%;
}

.news-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(0, 0, 0, 0.85), transparent 60%);
}

.news-content {
  position: absolute;
  bottom: 32rpx;
  left: 32rpx;
  right: 32rpx;
  z-index: 2;
}

.news-title {
  color: #fff;
  font-size: 36rpx;
  font-weight: 700;
  line-height: 1.4;
  display: block;
  margin-bottom: 16rpx;
}

.news-meta {
  display: flex;
  align-items: center;
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.6);
}

.section-footer {
  text-align: center;
  padding: 24rpx;
  color: rgba(255, 255, 255, 0.5);
  font-size: 26rpx;
}
```

### 6.3 详情页新增样式

```css
/* detail.wxss 新增 */
.fetched-tag {
  display: inline-block;
  padding: 4rpx 16rpx;
  background: rgba(76, 217, 100, 0.2);
  border-radius: 16rpx;
  font-size: 22rpx;
  color: #4cd964;
  margin-left: 16rpx;
}

.content-body {
  /* rich-text 的容器，清除默认样式 */
}

.content-body >>> .rich-text {
  color: rgba(255, 255, 255, 0.85);
  font-size: 30rpx;
  line-height: 1.8;
}

.content-body >>> image {
  max-width: 100%;
  border-radius: 16rpx;
}
```

> 注意：小程序 wxss 不支持嵌套 `>>>`, 但 rich-text 内容渲染不受 wxss 类名作用域限制。上面的写法在原生 h5 编译会忽略；如需严格控制 rich-text 内部样式，需后端在 content_html 中嵌入 inline style。

---

## 七、小程序发布配置

### 7.1 app.json 改动

```json
{
  "pages": [
    "pages/home/home",
    "pages/detail/detail",
    "pages/all-news/all-news",
    "pages/sources/sources",
    "pages/settings/settings"
  ],
  ...
}
```

**删除** `"pages/ai/ai"` 一行。

### 7.2 app.js 改动

`globalData` 沿用 v1，不需改。

### 7.3 components 复用

**全部沿用**，不新增组件。删除 `components/atoms/` 可选（虽然没引用但留着不报错即可；为简洁可一并删除）。

> 注意：`app.json` 的 `usingComponents` 里没有声明 `atoms`，所以这些组件其实是"死代码"，不影响运行。

---

## 八、数据流图（小程序视角）

```
┌──────────────────┐
│   pages/home     │
│   (按来源分组)   │
└─────────┬────────┘
          │ GET /api/v2/home
          ↓
┌──────────────────────────────────────┐
│ news_articles WHERE source_id IN (...)│
│           AND published_at >= -24h   │
│           GROUP BY source_id         │
└──────────────────────────────────────┘

┌──────────────────┐
│  pages/detail    │
│  (D + C 兜底)   │
└─────────┬────────┘
          │ GET /api/v2/news/{id}
          ↓
┌──────────────────────────────────────┐
│ 1. 查 news_articles 主行             │
│ 2. 查 news_contents（缓存命中？）    │
│    ├─ 命中：return content_html      │
│    └─ 未命中 + summary < 100字：     │
│       调 Crawler 抓详情页            │
│       写入 news_contents (TTL 24h)   │
└──────────────────────────────────────┘

┌──────────────────┐
│ pages/sources    │
│  (自定义+类型)   │
└─────────┬────────┘
          │ POST /api/v2/sources/custom
          │ body: { source_type, url, name? }
          ↓
┌──────────────────────────────────────┐
│ 1. 校验 source_type ∈ {rss, crawler} │
│ 2. 检查 URL 是否已存在               │
│ 3. 根据类型 fetch 测试 1 条          │
│    ├─ rss: feedparser 解析           │
│    └─ crawler: GenericHTMLCrawler    │
│ 4. 写入 news_sources                 │
└──────────────────────────────────────┘
```

---

## 九、与 v1 的兼容性

| 维度 | 兼容性策略 |
|------|-----------|
| 域名 | 完全复用，无需在公众平台改白名单 |
| localStorage key | 沿用 `digest_cache`、`settings`、`read_history`。`settings` 内的 `newsCount` 字段成为"遗留"，不会读也不会写 |
| 组件库 | 全部沿用，无 breaking change |
| 用户体验 | 用户重新打开 App 会发现：① AI 分析按钮没了 ② 首页变成按来源分组 ③ 详情页直接显示原文 |

---

## 十、未实现事项

- 推送通知 UI 占位保留，后端未实现
- 阅读历史功能（v1 也没实现）
- 个性化推荐（v1 已有弱实现，新版删除）

---

## 十一、验收清单

开发完成后，按以下清单在阿里云+小程序调试器验证：

- [ ] 小程序首页打开，自动按来源分组展示 5 个源
- [ ] 每个源展示最多 3 条新闻，且按发布时间排序（最新在前）
- [ ] 点击某个源 section 的"查看全部"按钮，跳到 all-news，仅显示该源
- [ ] 点击新闻卡片，进详情页，**优先展示详情页正文**（如果后端抓到）
- [ ] 摘要 < 100 字的新闻，详情页会触发后端按需抓取（首次进入有 loading）
- [ ] 同一新闻 24h 内二次访问，应直接命中 news_contents 缓存（响应快）
- [ ] 新闻源管理页，选择"网页爬虫"类型后能成功添加一个 HTML 源
- [ ] 新闻源管理页，添加自定义 RSS 源依然能成功（验证向后兼容）
- [ ] 设置页不再显示"首页推荐新闻数量"
- [ ] home 页的 swiper 在每个 source section 内部独立滑动，不影响其他 section
- [ ] 阿里云生产域名加载所有数据正常（验证 `API_BASE` 切换生效）

---

## 十二、风险与缓解

| 风险 | 缓解 |
|------|------|
| 首页5×3=15条，但实际某些源不足3条 | UI 已设计为空状态展示，group.items 为空时整个 section 不渲染 |
| rich-text 渲染可能不稳定（不同小程序的 rich-text 行为有差异） | 后端将 content_html 中的图片 URL 走代理；线下用 iOS/Android 两端验证 |
| 老用户 localStorage 里残留 `newsCount` 字段 | 不影响新逻辑，settings 对象兼容存在该字段 |
| 网络差时按需抓详情页（首次进入）会慢 | 详情页 loading 体验沿用 v1，可接受 |
| 通用 HTML 爬虫对自定义源效果差 | UI 在类型选择卡里明示"通用 HTML 提取，效果有限"，引导用户优先用 RSS |

